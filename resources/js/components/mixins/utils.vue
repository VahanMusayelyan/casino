<script type="text/babel">
    import { __, config } from '../../utils'

    export default {
        methods: {
            __(string) {
                return __(string);
            },
            config(string) {
                return config(string);
            }
        }
    }
</script>